import { createTheme } from "@mui/material/styles";
export const GolbalTheme = createTheme({
  typography: {
    fontFamily: "LineSeed",
  },
  status: {
    danger: "#EB455F",
  },
  palette: {
    primary: {
      main: "#2B3467",
      contrastText: "#fff",
    },
    neutral: {
      main: "#3b3b3b",
      contrastText: "#fff",
    },
  },
});
