import { createTheme } from "@mui/material/styles";
export const GolbalTheme = createTheme({
  typography: {
    fontFamily: "LineSeed",
  },
  palette: {
    primary: {
      main: "#01B469",
      contrastText: "#fff",
    },
    neutral: {
      main: "#3b3b3b",
      contrastText: "#fff",
    },
  },
});
