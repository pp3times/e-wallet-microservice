import { createTheme } from "@mui/material/styles";

export const Theme = createTheme({
  status: {
    danger: "#EB455F",
  },
  palette: {
    primary: {
      main: "#20B367",
      contrastText: "#fff",
    },
    neutral: {
      main: "#3b3b3b",
      contrastText: "#fff",
    },
  },
});
